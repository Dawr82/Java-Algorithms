pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                sh "uname -a"
            }
        }
        stage('test') {
            steps {
                sh "uptime"
            }
        }
    }
}